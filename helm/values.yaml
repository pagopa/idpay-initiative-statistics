microservice-chart:
  namespace: "idpay"
  nameOverride: ""
  fullnameOverride: ""

  livenessProbe:
    httpGet:
      path: /actuator/health/liveness
      port: 8080
    initialDelaySeconds: 120
    failureThreshold: 15
    periodSeconds: 10

  readinessProbe:
    httpGet:
      path: /actuator/health/readiness
      port: 8080
    initialDelaySeconds: 120
    failureThreshold: 15
    periodSeconds: 10

  deployment:
    create: true

  service:
    create: true
    type: ClusterIP
    port: 8080

  ingress:
    create: true
    path: /idpay/idpayinitiativestatistics/(.*)

  serviceAccount:
    create: false
    annotations: { }
    name: ""

  podAnnotations: { }

  podSecurityContext:
    seccompProfile:
      type: RuntimeDefault

  securityContext:
    allowPrivilegeEscalation: false

  envConfig:
    MONGODB_DBNAME: "idpay"
    APPLICATIONINSIGHTS_ROLE_NAME: "idpayinitiativestatistics"
    APPLICATIONINSIGHTS_INSTRUMENTATION_LOGGING_LEVEL: "OFF"
    APPLICATIONINSIGHTS_INSTRUMENTATION_MICROMETER_ENABLED: "false"

  envConfigMapExternals:
    idpay-common:
      TZ: TZ
    idpay-eventhub-00:
      KAFKA_BROKER_00: kafka_broker
      KAFKA_SASL_MECHANISM: kafka_sasl_mechanism
      KAFKA_SECURITY_PROTOCOL: kafka_security_protocol
      KAFKA_ONBOARDING_OUTCOME: idpay_onboarding_outcome_topic
      KAFKA_ONBOARDING_OUTCOME_CONSUMER_GROUP: TODO
    idpay-eventhub-01:
      KAFKA_BROKER_01: kafka_broker
      KAFKA_REWARD_SASL_MECHANISM: kafka_sasl_mechanism
      KAFKA_REWARD_SECURITY_PROTOCOL: kafka_security_protocol
      KAFKA_REWARD_RESPONSE_TOPIC: idpay_transaction_topic
      KAFKA_REWARD_RESPONSE_CONSUMER_GROUP: TODO


  envSecret:
    #    APPLICATIONINSIGHTS_CONNECTION_STRING: appinsights-instrumentation-key
    MONGODB_URI: mongodb-connection-string
    KAFKA_OUTCOME_SASL_JAAS_CONFIG: evh-idpay-onboarding-outcome-idpay-onboarding-outcome-consumer-jaas-config-idpay-00
    REWARD_RESPONSE_SASL_JAAS_CONFIG: evh-idpay-transaction-idpay-transaction-consumer-jaas-config-idpay-01